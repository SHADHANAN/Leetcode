class Solution:
    def search(self, nums: List[int], target: int) -> int:
        i=0
        n=-1
        while(i<len(nums)):
            if(nums[i]==target):
                n=i
                break 
            else:
                i+=1
        if(n==-1):
            return -1
        else:
            return n
