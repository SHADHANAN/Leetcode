class Solution:
    def findKthPositive(self, arr: List[int], k: int):
        j = 0
        num = 1

        while True:
            if j < len(arr) and arr[j] == num:
                j += 1
            else:
                k -= 1
                if k == 0:
                    return num
            num += 1
