class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        l=0
        while(l<len(nums)):
            nums[l]=nums[l]**2
            l+=1
        nums.sort()
        return nums
        
