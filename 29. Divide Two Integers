class Solution:
    def divide(self, dividend: int, divisor: int) -> int:
        ans=int(dividend/divisor)
        max=2**31-1
        min=-2**31
        if(ans>max):
            return max
        if(ans<min):
            return min
        return ans
