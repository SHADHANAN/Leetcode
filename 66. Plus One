class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        s=""
        l=[]
        for i in range(len(digits)):
            s+=str(digits[i])
        s=int(s)+1
        for i in str(s): 
            l.append(int(i))
        return l
