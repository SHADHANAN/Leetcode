class Solution:
    def findPeakElement(self, nums: List[int]) -> int:
        i=0
        k=len(nums)-1
        while(i<k):
            mid=(i+k)//2
            if nums[mid]>nums[mid+1]:
                k=mid
            else:
                i=mid+1
        return i
